# Memba

Memba is membership management system build with laravel framwork 5.2


## Installation
1. installing packages by running `composer install` and `npm install`
2. compile assets by running `gulp`